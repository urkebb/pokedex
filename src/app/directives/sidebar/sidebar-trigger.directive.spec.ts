import { SidebarTriggerDirective } from './sidebar-trigger.directive';

describe('SidebarTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
